#include <iostream>
#include <string>
#include <cstring>
#include <vector>

#define FOR(i,n,m) for(int i=n; i<m; i++)
typedef long long ll;

using namespace std;

ll fibo1(ll n);
ll fibo2(ll n);
ll fibo3(ll n);
