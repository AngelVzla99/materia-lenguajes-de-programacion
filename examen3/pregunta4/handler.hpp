#include <bits/stdc++.h>

#define FOR(i,n,m) for(int i=n; i<m; i++)

typedef long long ll;

using namespace std;

int addClass( string name, string super, vector<string> resto );
vector<pair<string,string>> getTable( string name );
void cleanAllClass();
